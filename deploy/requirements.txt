# Tested with Python 3.11 (use .venv311 on Windows)
ultralytics
opencv-python
numpy

# ONNX Runtime - REQUIRED for Raspberry Pi (use .onnx model to avoid PyTorch ARM issues)
onnxruntime  # If pip fails: try `pip install onnxruntime==1.16.3` or conda

# Optional (trackers) - may require build tools on Windows
# lap  # use `conda install -c conda-forge lap` or a prebuilt wheel if pip fails

# WebRTC streaming (for webrtc_server.py)
# Required: aiortc, aiohttp, av
# Install guide:
# - Windows (Python 3.11 recommended): pip install aiortc aiohttp av
#   If 'av' fails, use conda: conda install -c conda-forge av
# - Raspberry Pi: pip install aiortc aiohttp av (may need system libs - see README)
aiortc
aiohttp
av  # PyAV - if pip install fails on Windows, use: conda install -c conda-forge av

# MQTT support for detection alerts
paho-mqtt

